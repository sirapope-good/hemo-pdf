Packed Hemopro Report Packages (`.hprp`). Runtime scans this folder first, then falls back to `assets/templates/reports/`.

Use HPRP Studio (`/hprp-studio`) or `POST /api/hprp/pack-from-templates` to regenerate these files from the unpacked JSON templates.
