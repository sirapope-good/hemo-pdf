# JSZip (vendored)

`jszip.min.js` — [JSZip 3.10.1](https://github.com/Stuk/jszip) (MIT).

Used by HPRP Studio for Import/Export `.hprp` (ZIP of `manifest.json` / `layout.json` / `labels.*.json`) without a CDN.
